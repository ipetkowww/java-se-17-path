public class Program {

    public static void main(String[] args) {
        //Create an object of class Alarm and assign it to a variable. (You must pass it a string when you create it.)
        Alarm alarm = new Alarm("Wake up alarm.");

        //Call the method turnOn() on the object variable alarm.
        alarm.turnOn();

        //Define a null reference of type String.
        String _null = null;

        //Define a constant empty string.
        final String constant = "";
    }

}
